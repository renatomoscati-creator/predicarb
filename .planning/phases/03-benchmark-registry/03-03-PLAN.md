---
phase: 03-benchmark-registry
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - README.md
autonomous: true
requirements:
  - BENCH-05

must_haves:
  truths:
    - "README.md has a section titled 'Adding a New Benchmark Source'"
    - "The section shows a working ~10-line code example using registry.register()"
    - "The example imports from src.benchmark.registry and defines a concrete factory function"
    - "README.md also documents the two built-in sources (constant, zq) with example flags"
  artifacts:
    - path: "README.md"
      provides: "Benchmark extension guide section"
      contains: "registry.register"
  key_links:
    - from: "README.md extension example"
      to: "src/benchmark/registry.py"
      via: "from src.benchmark.registry import registry"
      pattern: "registry\\.register"
---

<objective>
Add a "Adding a New Benchmark Source" section to README.md documenting the registry extension pattern, plus a short reference for the two built-in benchmark sources.

Purpose: BENCH-05 — the README is the extension contract for future contributors. The example must work against the actual registry API created in 03-01.
Output: Updated README.md.
</objective>

<execution_context>
@/Users/renatomoscati/.claude/get-shit-done/workflows/execute-plan.md
@/Users/renatomoscati/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-benchmark-registry/03-01-SUMMARY.md

<interfaces>
<!-- Registry API as implemented in 03-01. -->

```python
# src/benchmark/registry.py

class BenchmarkRegistry:
    def register(self, key: str, factory: Callable[..., Any]) -> None: ...
    def get(self, key: str, **kwargs: Any) -> Any: ...
    def keys(self) -> list[str]: ...

registry = BenchmarkRegistry()   # module-level singleton
registry.register("constant", _constant_factory)
registry.register("zq", _zq_factory)
```

Factory signature convention:
- Accepts **kwargs (tolerant of unrecognised keys)
- Returns any object with `get_prob(ts_utc: datetime) -> float`
- Raises ValueError for missing required params

BenchmarkProvider Protocol (src/benchmark/csv_benchmark.py):
```python
class BenchmarkProvider(Protocol):
    def get_prob(self, ts_utc: datetime) -> float: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add benchmark extension guide to README.md</name>
  <files>README.md</files>
  <action>
    Read the current README.md first. If it does not exist, create a minimal one with the required sections. If it exists, add the benchmark section in the right place (after CLI reference, before or as part of a "Extending" or "Contributing" section).

    Add the following content as a new top-level section `## Adding a New Benchmark Source`:

    ```markdown
    ## Adding a New Benchmark Source

    PredicArb uses a central `BenchmarkRegistry` to map source keys to factory functions. Adding a new source requires **only** adding an entry to the registry — no changes to the CLI are needed.

    ### Built-in sources

    | Key | Flag example | Notes |
    |-----|-------------|-------|
    | `constant` | `--benchmark-source constant --benchmark 0.42` | Fixed probability, useful for backtesting |
    | `zq` | `--benchmark-source zq --zq-meeting-date 2026-06-18` | CME 30-Day Fed Funds Futures implied P(cut), ~15 min delay via Yahoo Finance |

    ### Extension example (~10 lines)

    Create your factory and register it at import time. The factory receives **kwargs
    forwarded from the CLI namespace and must return an object with `get_prob(ts_utc: datetime) -> float`.

    ```python
    # src/benchmark/my_source.py
    from datetime import datetime
    from src.benchmark.registry import registry


    class MyBenchmark:
        """Returns a fixed probability drawn from an external API."""

        def __init__(self, api_url: str) -> None:
            self._url = api_url

        def get_prob(self, ts_utc: datetime) -> float:
            import requests
            return requests.get(self._url).json()["probability"]


    def _my_factory(my_api_url: str = "", **_):
        if not my_api_url:
            raise ValueError("--my-api-url is required for --benchmark-source my-source")
        return MyBenchmark(my_api_url)


    registry.register("my-source", _my_factory)
    ```

    Then import your module at startup (e.g., add `import src.benchmark.my_source` to `src/cli.py`
    or your entry point) so the registration runs. From that point, passing
    `--benchmark-source my-source` to any `run` or `run-multi` command will use your factory.

    No edits to `_build_benchmark()` or any CLI parser are required.
    ```

    Place this section after any existing CLI command table and before any Development/Contributing section. If the README is empty or minimal, add appropriate surrounding sections (What it is, Quickstart, CLI commands) as stubs with a note that full docs are added in Phase 4. The benchmark section itself must be complete and accurate.
  </action>
  <verify>
    <automated>cd /Users/renatomoscati/predicarb && grep -c "registry.register" README.md && grep -c "Adding a New Benchmark Source" README.md</automated>
  </verify>
  <done>
    `grep "registry.register" README.md` returns at least 1 match.
    `grep "Adding a New Benchmark Source" README.md` returns exactly 1 match.
    The section includes the built-in source table and the ~10-line extension example.
  </done>
</task>

</tasks>

<verification>
- `grep "Adding a New Benchmark Source" README.md` → 1 match
- `grep "registry.register" README.md` → at least 1 match
- `grep "benchmark-source constant\|benchmark-source zq" README.md` → both built-in sources documented
- README renders correctly (no broken markdown — no unclosed code fences)
</verification>

<success_criteria>
1. README.md has a section "Adding a New Benchmark Source"
2. The section includes: built-in source table (constant, zq), ~10-line factory example, `registry.register()` call, note that no CLI edits are needed
3. Code example is syntactically valid Python and references the real registry API from 03-01
</success_criteria>

<output>
After completion, create `.planning/phases/03-benchmark-registry/03-03-SUMMARY.md`
</output>
