---
phase: 05-ci-ship
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/test.yml
autonomous: true
requirements:
  - CI-01
  - CI-02

must_haves:
  truths:
    - "Every push to main triggers the test suite on GitHub Actions"
    - "Every pull request triggers the test suite on GitHub Actions"
    - "CI runs on ubuntu-latest with Python 3.13"
    - "All 149 tests pass in CI"
  artifacts:
    - path: ".github/workflows/test.yml"
      provides: "GitHub Actions workflow definition"
      contains: "python-version"
  key_links:
    - from: ".github/workflows/test.yml"
      to: "pytest"
      via: "run: python -m pytest"
      pattern: "python -m pytest"
    - from: ".github/workflows/test.yml"
      to: "pyproject.toml"
      via: "pip install -e .[dev]"
      pattern: "pip install"
---

<objective>
Create the GitHub Actions CI workflow that runs the full pytest suite on every push to main and on every pull request.

Purpose: Keep the test suite green automatically as the project is made public. Provides confidence that contributions don't break existing behaviour.
Output: `.github/workflows/test.yml` — a single workflow file that is the only deliverable for this phase.
</objective>

<execution_context>
@/Users/renatomoscati/.claude/get-shit-done/workflows/execute-plan.md
@/Users/renatomoscati/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .github/workflows/test.yml</name>
  <files>.github/workflows/test.yml</files>
  <action>
    Create the directory `.github/workflows/` and write `test.yml` with the following exact configuration:

    - `name: Tests`
    - `on:` triggers: `push` to branch `main`, and `pull_request` (all branches)
    - Single job named `test`
    - `runs-on: ubuntu-latest`
    - `strategy.matrix` with `python-version: ["3.13"]` (single entry, satisfies CI-02)
    - Steps:
      1. `actions/checkout@v4`
      2. `actions/setup-python@v5` with `python-version: ${{ matrix.python-version }}`
      3. Install deps: `pip install -e ".[dev]"` — this uses pyproject.toml's `[dev]` optional-deps which includes pytest
      4. Run tests: `python -m pytest` (not `pytest` — matches project convention in CLAUDE.md)

    Do NOT add coverage flags, --tb flags, or extra pytest args — keep it minimal and matching the project's own test invocation.

    The `env` section is not needed; no secrets or env vars are required to run the test suite (tests use tmp_path fixtures and no real network calls per CLAUDE.md).
  </action>
  <verify>
    <automated>python3 -c "import yaml; w = yaml.safe_load(open('/Users/renatomoscati/predicarb/.github/workflows/test.yml')); assert 'push' in w['on']; assert 'pull_request' in w['on']; assert w['on']['push']['branches'] == ['main']; assert '3.13' in w['jobs']['test']['strategy']['matrix']['python-version']; assert w['jobs']['test']['runs-on'] == 'ubuntu-latest'; print('workflow VALID')"</automated>
  </verify>
  <done>`.github/workflows/test.yml` exists, triggers on push to main and on pull_request, matrix specifies python-version 3.13 and ubuntu-latest, installs via pip install -e .[dev], runs python -m pytest.</done>
</task>

</tasks>

<verification>
After the workflow file is created, verify locally:

1. File exists at `.github/workflows/test.yml`
2. YAML is valid (no syntax errors)
3. `on:` block contains both `push` (branches: [main]) and `pull_request`
4. `strategy.matrix.python-version` contains `"3.13"`
5. `runs-on: ubuntu-latest`
6. Install step uses `pip install -e ".[dev]"`
7. Test step uses `python -m pytest`

CI green badge verification happens after the commit is pushed to GitHub — that is the final gate for CI-01/CI-02.
</verification>

<success_criteria>
- `.github/workflows/test.yml` is committed and pushed to main
- GitHub Actions tab shows the workflow ran and all checks passed (green)
- `python-version: ["3.13"]` and `ubuntu-latest` satisfy CI-02
- Both `push` to main and `pull_request` triggers satisfy CI-01
</success_criteria>

<output>
After completion, create `.planning/phases/05-ci-ship/05-01-SUMMARY.md` with what was built, the final workflow YAML, and confirmation that CI is green.
</output>
